# Cluster configuration sourced by run_matrix.sh.
#
# ┌──────────────────────────────────────────────────────────────────────┐
# │  EDIT BEFORE RUNNING. Replace each <nodeN> with the FQDN from your   │
# │  CloudLab allocation (e.g., "hp076.utah.cloudlab.us"). The same      │
# │  hostnames appear in scripts/cluster.kdl — keep them in sync.        │
# └──────────────────────────────────────────────────────────────────────┘
#
# Precedence (highest first):
#   1. Values you set in this file (direct assignments below)
#   2. Script defaults in run_matrix.sh

COORD_NODE=<node0>

SERVER_NODES="<node1> \
              <node2> \
              <node3> \
              <node4> \
              <node5> \
              <node6>"

# Optional overrides (uncomment to use):
# REPO=$HOME/RDMA-Distributed-KV-Store
# YCSB_HOME=$REPO/ycsb-0.17.0
# RECORDS=50000
# OPS=10000
# VALUE_BYTES=1024
# THREADS=1
